# Extend the main fine-tuned PACT comparison to all three seeds

The user authorized training fine-tuned PACT for seeds 3104 and 3105, evaluating both, and comparing ACT, PACT-Finetune and PACT-Frozen at the end, with hourly manual health checks. Seed 3103 is already complete and remains unchanged.

Train two fresh policies and pretrained encoders independently, each for exactly 60,000 committed updates. Reuse seed 3103's full main method and byte-identical upstream sources: live 128-D CLS features, trainable stem/transformer, encoder AdamW group at 1e-5, minimum pooling and eight causal frames. Initialize both encoders from the same original pretrained checkpoint, never from the already fine-tuned seed3103 weights. Retain the original 240/40 split, batch eight, uniform starts, statistics, wrist images, state/actions, policy architecture and training math. Seed initialization, data loading and epoch RNG must all use the requested seed.

Keep the finished seed3103 files and original ACT/frozen PACT code, data, models and results unchanged. Seed-parameterized adapters live in new scripts; their changes from seed3103 are limited to seed/output paths and the previously tested host-capacity loader. Reuse verified immutable upstream modules and H5 data views through recorded paths. Check each seed's baseline policy configuration against the seed3103 configuration.

Evaluate the fixed final same-update policy/encoder pair on all 50 original final scenes assigned to its seed. Preserve successful sampling retry seeds, exact non-RGB initial states and existing RGB tolerance, the 900-action full horizon, 2 ms contact audit, geometry, gripper decoder and history100 controller. No validation-based checkpoint selection or outcome-based scene replacement. The existing seed3103 evaluator smoke and numerical train/live checks remain applicable; verify the new seed binding explicitly before launching.

The final denominator is 150 scenes per method, 450 raw trajectories total. Verify and reuse original ACT and PACT-Frozen evaluations for all three seeds, plus the completed fine-tuned seed3103 evaluations. New simulation work is exactly 100 fine-tuned evaluations. Check three-way scene/initial-state pairing and independently read all raw primary, pickup, lift, action and contact endpoints. Report per-seed and pooled task success, collision-free task success, pickup/placement failures, engagement and separate contact classes. Include paired gains/losses and variation across seeds. These are previously exposed regression scenes; this is the full main method rather than an isolated unfreezing experiment.

Preflight must verify both requested seed bindings, original policy configuration equality, real sample/RNG preservation, nonzero encoder gradients and actual updates, live causal parity, upstream bytes, strict initialization and checkpoint-pair checks. Retain a 300-update pilot per seed and resume its joint policy/encoder/optimizer/RNG checkpoint to 60,000. Hash the new code and protected inputs before supervised work. Record each parent-observed exit and retain failures.

Run one seed at a time, with training and simulation separated. Start evaluation with six workers, as measured in the completed seed3103 run. Retain VRAM85%, RAM80%, PID75% guards, a 10 GiB free-disk reserve and a new 48-hour per-seed execution deadline. Wait before loader process/thread allocation during host PID pressure. After training, use read-only cache advice on the completed training dataset before evaluation. Do not terminate unrelated processes. Keep minute-level resource telemetry and make hourly manual reviews of process identity, progress, checkpoints, losses, exit ledgers, resource usage and protected hashes; investigate failures immediately when observed. No external messaging or publishing.
