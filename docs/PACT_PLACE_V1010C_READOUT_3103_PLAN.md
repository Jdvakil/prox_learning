# V10.10c: main-branch PACT readout, seed 3103

The user authorized copying main's fine-tuning solution byte for byte, integrating it into this architecture, retraining PACT and comparing seed3103 with its old frozen-encoder PACT. This is a new experiment after V10.10b stopped at gate B. Its gates and expired schedules do not authorize or constrain this separate requested intervention.

Source revisions: prox_learning `af28690` and its exact ACT submodule `d956cbcab832a39e83f7face8b533a0c1bfad06c`. Complete upstream files are retained without edits under `diagnostics_output/pact_place_v1010c_readout_s3103/upstream`, with Git blob IDs and SHA256 in `upstream_manifest.json`. The executable encoder, causal-history construction, readout forward, optimizer encoder group and encoder serialization come directly from these sources. Repository-specific adapters remain separate.

Main's full method uses live 128-D CLS tokens, one per sensor, with encoder gradients entering the same policy optimizer. The old arm uses frozen 32-D embeddings. This comparison therefore measures the full main-branch method; it cannot isolate unfreezing alone from the changed tap and input pooling.

Use our existing pretrained encoder as the default initialization, pending the user's response to the initialization question. The particular pretrained checkpoint documented on main is unavailable here. Record the exact selected initialization before training; never substitute already fine-tuned hallway weights silently.

Train one new PACT policy from scratch with seed3103 for exactly60000 committed optimizer updates (2000 epochs ×30 batches), batch8, original240 training /40 validation identities, original train-only action/qpos statistics, uniform starts and no acquisition-window mixture. Preserve wrist240×320, 9-D state, 8-D action, chunk100, 7/7 transformer layers, hidden512, feedforward3200, 8 heads, KL10, AdamW and lr/backbone1e-5. The encoder gets main's optimizer parameter group at lr1e-5. Initialize the pretrained encoder before resetting the policy RNG to3103. Do not retrain ACT.

Use main's minimum pooling over four native subframes, followed by its eight consecutive control-frame causal window, initial left padding, repeated pooled subframes and20cm closeness map. Create new H5 views with byte-identical external links to the original supervision/images and new minimum-pooled proximity. This avoids upstream's alternate mean-pooling path for native5-D files. Keep our existing canonical sensor order in both train and eval; main's shared per-sensor encoder is permutation equivariant and the policy sensor slots retain their meanings. Verify these assertions numerically.

Evaluate the fixed final policy and same-update encoder at60000, never a validation-selected or mismatched pair. Keep the original history100 exponential chunk aggregation, absolute joint/0-or255 gripper decoder,900-action/901-observation horizon,2ms contact audit, exact Cup_10 geometry and four-object sampler. The main evaluator's alternate chunk gating, cameras, geometry and task settings are outside this intervention.

Use all50 original seed3103 final scenes, actual successful sampling seeds and retries. Reuse their retained frozen PACT outcomes only after raw/hash/identity verification. Run one frozen replay as a binding smoke, retain it separately and do not substitute it into the denominator. Fine-tuned evaluations compare exact non-RGB initial state/configuration and existing RGB tolerance (max2 levels; changed-channel fraction<=.001) against each original. Mismatches must be explained or repaired before claims. These are exposed regression scenes, not a fresh confirmatory final test.

Required preflight: byte-copy hashes; strict pretrained reload;128-D live forward; finite nonzero encoder gradients and actual stem updates; no auxiliary-head loss; exact original non-proximity sample tensors/RNG; min-pooling and causal train/eval parity including reset; joint policy/encoder optimizer/RNG resume equivalence; same-update checkpoint pairing and rejection of missing/wrong encoder; initial physical matching and full horizon. A short retained training pilot must establish throughput and resources before the rest of60000 updates.

Report task success, collision-free task success, exclusive/raw pickup failures, lift/engagement, post-lift failures, hazard/clutter union frames and avoidance, other contact classes, and matched wins/losses. Compare against the original frozen seed3103 PACT (20/50 task successes), not the later acquisition candidate. The initial draft mistakenly used ACT's19/50; raw preflight caught this before contract freeze or training. Report actual denominators and all infrastructure failures separately. No claims about the other two seeds, population significance or a pure encoder-unfreezing effect.

Keep all original code/data/models/reports unchanged. New work lives under the V10.10c directory and new scripts/tests. Retain atomic resumable policy+encoder+optimizer+RNG checkpoints and parent-observed exits. Do not launch training and simulator work concurrently. Start evaluation at8 workers, permit10 only after measured capacity, enforce VRAM85%,RAM80%,PID75% guards and10GiB disk reserve. Use in-process NVML. Sample resources every minute and honor requested hourly manual checks during long work. Record a new48h execution deadline before the first supervised job; stop and report if resource/time limits prevent completion. No unrelated process termination or external messaging.
