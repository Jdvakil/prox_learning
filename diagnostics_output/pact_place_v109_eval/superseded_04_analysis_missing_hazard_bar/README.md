# Superseded V10.9 analysis, attempt 02

Retained, not deleted. `verified: true`, 0 problems — its numbers are correct as
far as they go, and the primary endpoint is unchanged in the operative analysis.

It omitted the `hazard_bar` class from the reported endpoints. In this
environment `hazard_bar` is `pact_intrusion_*`, the sampler-injected intrusion
panel — the corridor hazard itself, the dominant non-target contact class, and
the main driver of collision-free task success. Reporting the taxonomy without
it left the largest effect in the data visible only inside the failure counts.

The operative analysis adds `hazard_bar_contact_episode`,
`hazard_free_task_success`, `hazard_bar_frames_total`, and pairs both
hazard-bar and clutter contact alongside the success endpoints.
