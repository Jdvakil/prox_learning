# Effective280 training configuration binding and one startup retry

The first ACT seed3103 uniform continuation exited1 after cloning the frozen update60000 bundle and before importing the trainer. The adapter compared `wrist288_config_sha256` to the preserved base288 config (`c3a2a244…`). All six actual historical source bundles were trained under the effective280 amendment (`da3388ab…`), as their bundles and progress receipts agree. The historical wrist280 launcher explicitly patches the original trainer's binding routine to that effective configuration.

The correction binds the existing effective280 configuration as an immutable input and retains its digest in every new training bundle. The new experiment contract digest remains separate metadata. Original model, optimizer, RNG, dataset, split, statistics, architecture and training operations remain unchanged. No optimizer updates occurred in the failed attempt.

The failed job, launch, real observed exit receipt, log and fork receipt are preserved under `infrastructure_attempts/train_ACT_3103_uniform63000/attempt_00/`. `training_recovery.json` identifies exactly one permitted infrastructure retry and the original mutable working-resume checksum. The retry independently compares that working model, optimizer, RNG and provenance recursively against the frozen source. Its latest resume remains a temporary working artifact and can be removed only following a verified update63000 checkpoint and parent-observed zero exit.

Storage projection now reports two time phases. The future rollout phase credits only the known working resume that must be removed after a valid training branch; the training phase separately reserves all remaining final models plus enough space for at most two atomic working generations. Both phases preserve the same10GiB reserve. Failure to complete training retains the latest resume and prevents later rollout launches.

Preflight executes the original ACT and PACT trainer startup, source resume/provenance checks, actual fixed loaders, one validation forward and one training forward/backward. Optimizer.step is intercepted to fail before any update; these are zero-update bootstrap fixtures, not continuation branches or scientific rollout observations. Full source bundles and the failed working state are independently checked. No sampling probability, window, numerical gate, scene, checkpoint choice, denominator or deadline changes.
