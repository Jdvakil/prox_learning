# Bounded thread-resource recovery

The twelfth training branch, PACT seed3105 acquisition63000, exited1 at08:45:13UTC with `RuntimeError: can't start new thread` in DataLoader validation-iterator shutdown. The parent observed that exit, preserved the latest update61500 bundle, independently verified completed artifacts and closed at08:47:46UTC. Eleven final63000 branches remain valid. Sampled shared cgroup PIDs rose420→1300→2358; RAM remained below58% and no new OOM kill occurred. The exact producer of the transient thread pressure is not established. This is an infrastructure failure, not a tested rejection of the sampling intervention.

The partial attempt is retained under `infrastructure_attempts/train_PACT_3105_acquisition63000/attempt_00`. Its working bundle remains in the canonical new directory without duplication; the archive retains the exact SHA256 reference and all other files. The committed state is61500 and its first12000 episode/start samples must exactly equal the paired ACT candidate prefix. Epoch2057 reached61740 before the next validation iterator failed;240 uncommitted updates and their sample/log evidence are retained separately and replayed from the committed61500 model/optimizer/RNG state. The final model still contains precisely transitions60001–63000. Actual executed optimizer invocations include the240 discarded/replayed transitions, which must accompany final reporting.

This uses one retry for this job and the second retry overall, within the frozen limits of one/job and12 overall. It preserves the same contract, model, physical/data identities, sampling probability, four DataLoader workers, seed/epoch sampling semantics and final63000 endpoint. No experiment source file is changed. The versioned recovery launcher uses the existing parent Pool to obtain actual child exits and preserve its exception/deadline draining behavior. It caps subsequent evaluation pools at10 workers following the thread failure; all12 smoke jobs and every frozen evaluation row remain scheduled. Serial training remains one worker. The unchanged time and10GiB storage gates are rechecked, with three settled-resource samples at one-minute intervals. No failed scientific rollout is retried.

After the single resume succeeds, the unchanged training gate checks all six ACT/PACT sample-stream pairs. The existing B→C→D functions then execute, stopping at their frozen scientific gates or the original resource/time bounds. The48h deadline remains anchored to the original T0. The old closure is marked superseded only when the recovery parent is actually running; `run_status.json` points to the live progress and final closure.
