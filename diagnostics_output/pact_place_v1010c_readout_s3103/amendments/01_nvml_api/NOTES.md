The first supervisor exited before any child launch because the NVML memory API was called with integer2 instead of pynvml.nvmlMemory_v2. Corrected only the resource call and added a real no-subprocess check (passed). Source encoder modules remain byte-identical. Original contract, source, terminal status and logs are retained in prior/. The deadline and scientific settings are unchanged.
