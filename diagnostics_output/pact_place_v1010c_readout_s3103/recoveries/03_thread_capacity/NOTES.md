# Resume after transient host thread exhaustion

The root hourly review at20:41UTC found the full-training worker had exited1 at20:29UTC after `multiprocessing.Queue` could not start its shutdown thread. Parent2351886 observed and retained that exit. Thread usage had stayed around10% for45minutes, then rose to88% within two minutes; the current review found it back below10%. RAM peaked around72%, VRAM stayed17.3%, and no new OOM kill occurred. The producer of the shared host thread burst is not established. No unrelated processes were stopped.

The last atomic joint checkpoint is update15600 / epoch519. The process logged two more complete epochs through15660; its traceback occurred when the training DataLoader exhausted the following epoch. The two logged discarded epochs and up to30 additional unlogged updates are outside the retained training trajectory. Resume replays from15600 with the exact saved policy, encoder, optimizer and RNG, deterministic epoch seeds and unchanged four-worker loader. Final committed training remains60000.

The existing resume bundle is preserved by a hard link under prior/ before its next atomic replacement. Source code and the scientific contract remain unchanged, so its contract hash remains valid. This separately hashed recovery launcher owns the same parent lock, verifies the original contract, skips the already completed frozen smoke and300-update pilot, and launches the same full-training command under a new attempt identity. It then uses the same eight-worker evaluation and comparison routines. Failed attempt paths, the valid/invalid ledgers, and the original48h deadline remain intact.

The root also compared the two previously logged discarded epochs with the replay. The first resumed validation is bit-identical; replayed training losses differ by1.65e-5 and4.22e-5, and the following validation loss differs by2.68e-4. The retained states and RNG were restored correctly under unchanged epoch seeding. Full GPU training replay is not claimed bit-identical; the precise source of the subsequent numerical differences was not isolated. The comparison is preserved in replayed_epochs.json.
