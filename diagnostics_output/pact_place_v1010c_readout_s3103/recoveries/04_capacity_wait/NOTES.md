# Wait for host capacity without changing training data or optimizer math

The second infrastructure failure occurred at21:02UTC when the original four-worker validation loader could not fork because the shared host PID limit was reached. Parent2411027 observed worker2411134 exit1. The20700-update checkpoint had already been written; there were no further optimizer updates before the failed validation loader start.

The root's additional monitor captured3399 Git processes under app-server PID3077567 at21:01:52UTC. Cgroup usage was3781/3840. Training itself had5 threads then, and earlier samples showed10 training threads plus four three-thread data workers. The spike was outside the experiment's process tree. All counts and parent identities are retained in root_review/thread_pressure.jsonl. The root did not stop unrelated processes or modify the app server.

An operational wrapper now waits before constructing an original DataLoader iterator and before requesting each next batch, including end-of-epoch shutdown. It starts waiting at50% PID use and resumes below40%; a30-minute unresolved wait stops the worker. This avoids spawning or shutting down data-worker queues during the captured bursts. It changes no tensors, sampling, worker count, validation windows, RNG, gradients, optimizer, or upstream encoder source. A real full epoch test compared all30 train and5 validation batches bit for bit with the original loaders at the resume epoch, and checked unchanged parent Torch/NumPy RNG. A second test exercised pressure and recovery. Both passed.

The original scientific contract and training source remain unchanged, preserving checkpoint contract validity. This recovery's separately hashed launcher and data-availability wrapper are recorded in recovery.json. Resume starts at20700 using the same joint checkpoint and original optimizer/RNG restoration. The failed run, checkpoint hard link, ledger records, and original48h deadline remain retained. The final comparison will disclose both training infrastructure failures and will not claim bit-identical uninterrupted GPU training.
